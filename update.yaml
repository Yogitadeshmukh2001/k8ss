- name: update webserver
  hosts: all
  become: true
  
  tasks:
    - name: change web page
      copy:
        src: index.html
        dest: /var/www/html/index
    - name: start service
      systemd:
        name: apache2
        state: started
        enables: true    